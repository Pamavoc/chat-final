import './main.scss'
import Socketclient from '../static/js/Socketclient'

window.addEventListener('load', () => {})

window.addEventListener("DOMContentLoaded", (e) => {
    const sc = new Socketclient();
})